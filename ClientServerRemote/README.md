# Client-Server on remote machines
## (On same network)

Run these codes on different machines.  
Enter server's IP address in client's code.  
You can take server IP as an input from user instead of hardcoding it.
